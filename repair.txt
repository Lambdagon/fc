// This text document is used by FCDownloader.
"repair"
{
	// Delete the following files after pressing the repair button.
 	"delete"
	{
		"cfg/skill.cfg"
		"cfg/autoexec.cfg"
		"bin/client.dll"
		"bin/server.dll"
	}
	// Restore the following files if the delete process is finished
	"restore"
	{
		"cfg/skill.cfg"
		"cfg/autoexec.cfg"
		"bin/client.dll"
		"bin/server.dll"
	}
}